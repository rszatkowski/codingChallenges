import { stringsPage } from "./strings.js";
import { arraysPage } from "./arrays.js";
import { settingsPage } from "./settings.js";

// Map of section identifiers to their page configuration.
export const pagesById = {
  strings: stringsPage,
  arrays: arraysPage,
  settings: settingsPage,
};
