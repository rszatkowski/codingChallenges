// Copy and placeholder content for the Arrays page.
export const arraysPage = {
  summary: "Map out array transformations, loops, and data reshaping ideas.",
  placeholder: `// Sketch an approach.\n// Example:\n// const doubled = numbers.map((n) => n * 2);`,
};
